version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "${{ steps.task-def.outputs.task-definition }}"  # 동적으로 업데이트된 Task Definition 사용
        LoadBalancerInfo:
          ContainerName: "Aurora-ECS-Container-Seoul"
          ContainerPort: 8080
